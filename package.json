{
  "name": "vibertthio.github.io",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Vibert Thio's personal website.",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "screenshots": "node scripts/capture-screenshots.mjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "marked": "^18.0.5",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@vitejs/plugin-react": "^6.0.2",
    "gh-pages": "^6.3.0",
    "vite": "^8.0.16"
  }
}
